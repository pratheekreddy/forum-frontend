version: '3.7'

services:
  frontend:
    container_name: frontend
    build:
      context: .
      dockerfile: dockerfile
    ports:
      - 3001:3000
    stdin_open: true
    environment:
      - CHOKIDAR_USEPOLLING=true
    command: npm start